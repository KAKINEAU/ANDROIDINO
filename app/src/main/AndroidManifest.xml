<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.libreapps.projetino">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetIno">
        <activity android:name=".ajouter_Recette"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".Mon_Compte"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".Profile"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity
            android:name=".Mes_recettes"
            android:label="@string/title_activity_mes_recettes"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".Menu"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".inscription"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".Connexion"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".AUrevoir"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity android:name=".accueil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>